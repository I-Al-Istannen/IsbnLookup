<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto">

  <item
    android:title="@string/actionbar_settings_name"
    app:showAsAction="never"
    android:id="@+id/action_bar_settings"/>

  <item
    android:title="@string/actionbar_scan_name"
    app:showAsAction="ifRoom"
    android:id="@+id/action_bar_scan"
    android:icon="@drawable/ic_barcode_scan"
    android:onClick="onInitiateScan"
    />
  <item
    android:title="@string/actionbar_history_name"
    app:showAsAction="ifRoom"
    android:id="@+id/action_bar_history"
    android:icon="@drawable/ic_history"
    android:onClick="onShowHistory"
    />
</menu>